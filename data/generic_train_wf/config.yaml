fedscgen:
  workflow: "train" # train or correction
  data:
    adata: MHSPC.h5ad
    batch_key: batch
    cell_key: cell_type
  n_rounds: 2
  smpc: True # True or False
  model:
    init_model: model
    ref_model: model.pth
    hidden_layer_sizes: "800,800"
    z_dimension: 10
  train:
    lr: 0.01
    n_epochs: 1
    batch_size: 32
